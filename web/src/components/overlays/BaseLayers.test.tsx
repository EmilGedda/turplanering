import '@testing-library/jest-dom';

describe('BaseLayers', () => {
  it('runs', () => {
    expect(1).toEqual(1);
  });
});

describe('Layer', () => {
  it('runs', () => {
    expect(1).toEqual(1);
  });
});
